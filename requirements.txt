# Core async dependencies
asyncssh>=2.14.0
aiohttp>=3.9.0
aioconsole>=0.7.0

# HTTP requests (for data fetchers)
requests>=2.31.0

# UI and configuration
colorama>=0.4.6
python-dotenv>=1.0.0

# Optional: OpenAI (if using their API)
openai>=1.0.0

# NEW: Extended API search capabilities
# DuckDuckGo search
duckduckgo-search>=3.9.0

# Optional: For better JSON parsing
ujson>=5.8.0

# Optional: For SERP API (if you have a key)
google-search-results>=2.4.2